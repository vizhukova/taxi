export interface MapState {
    initial ?: boolean,
    searching ?: boolean,
    authorized ?: boolean,
    direction ?: string
}

// initial - is it initial load of map

// searching - is address searching now